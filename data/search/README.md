# Recherche hors ligne

Le fichier `nord-pas-de-calais.orplaces.sqlite` est genere localement depuis le
meme extrait OpenStreetMap `.osm.pbf` que le graphe routier :

```sh
./scripts/prepare_place_index.sh
```

Il n'est pas versionne dans Git.
