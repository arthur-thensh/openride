name: Temporary Android road label LOD v1

"on":
  push:
    branches:
      - "agent/ormap-road-label-lod-v1"

permissions:
  contents: read

jobs:
  android-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: "agent/ormap-road-label-lod-v1"

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Bootstrap native dependencies
        run: |
          ./scripts/bootstrap_sdl.sh
          ./scripts/bootstrap_sqlite.sh

      - name: Build production Android debug APK
        run: ./scripts/android_build.sh
